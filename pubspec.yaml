name: task4
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.2 <4.0.0'  # More precise SDK constraint

dependencies:
  bloc: ^9.0.0
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_screenutil: ^5.9.3

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/  # Specific font directory only (more efficient)

  fonts:
    - family: Nunito
      fonts:
        - asset: assets/fonts/Nunito-Regular.ttf
        # Example additional variants (uncomment if needed):
        # - asset: assets/fonts/Nunito-Italic.ttf
        #   style: italic
        # - asset: assets/fonts/Nunito-SemiBold.ttf
        #   weight: 600